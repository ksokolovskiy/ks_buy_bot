# Telegram Shopping List Bot

Простой Telegram бот для управления списком покупок с группировкой по отделам.

## Возможности
- ➕ Добавление товаров с выбором отдела
- 📋 Просмотр списка покупок, сгруппированного по отделам
- ✅ Отметка товаров как купленных (одним кликом в inline-клавиатуре)
- 👁 Показать/скрыть купленные товары
- 🗑 Удаление купленных товаров или отдельных позиций
- 🔒 Проверка доступа по списку User ID

## Установка и запуск

### 1. Подготовка
Создайте бота в [@BotFather](https://t.me/BotFather) и получите токен.
Узнайте свой Telegram User ID (например, через [@userinfobot](https://t.me/userinfobot)).

### 2. Настройка окружения
Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```
Заполните `BOT_TOKEN` и `ALLOWED_USERS` (ID пользователей через запятую).

### 3. Запуск через Docker (Рекомендуется)
```bash
docker-compose up -d
```

### 4. Запуск в venv (Локально)
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python bot.py
```

## Структура проекта
- `bot.py` — Основная логика бота
- `database.py` — Работа с SQLite
- `config.py` — Константы и настройки
- `data/` — Директория для БД (создается автоматически)
